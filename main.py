# The code is importing three Python libraries: pandas, numpy, and plotly.express. The `as` keyword is
# used to create an alias for each library, which allows for easier referencing of the library's
# functions and methods later in the code.
## importing libraries
import pandas as pd
import numpy as np
import plotly.express as px

